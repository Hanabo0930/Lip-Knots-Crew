# 旧画像・再送画像の比較 v0.7

- 管理者は案件ごとの提出ファイル一覧から、読めない画像を1枚選択します。
- 選択した画像IDを付けて再送依頼します。
- スタッフには元画像、理由、補足と「再送する」ボタンを表示します。
- 画像単位の再送は1ファイルだけ選択できます。
- 新しいファイルは既存の通し番号の続きで保存し、元画像は削除しません。
- 管理画面では元画像と再送画像を横並び表示し、確認後に完了します。

Driveファイルは公開リンクにしません。権限確認後に15分だけ有効なプレビューURLを発行し、バックエンドがDriveから中継します。
