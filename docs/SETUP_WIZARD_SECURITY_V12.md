# 導入ウィザードの安全設計

- 検査はGoogle Sheets APIの読取専用権限
- URLからスプレッドシートIDだけを抽出
- 秘密鍵・APIキー・実データを設定JSONへ含めない
- 検査結果は24時間で期限切れ
- 管理者権限と会社IDを確認
- 設定は下書き専用コレクションへ保存
- 自動有効化なし
- 操作は監査ログへ記録

## 新月作成

- 対象月単位の実行ロック
- 既存タブと同名の場合は停止
- 複製元と作成先が同じ場合は停止
- 書込後に再取得して検算
- エラー時は新しいタブだけ削除
- 削除にも失敗した場合は手動確認へ記録
