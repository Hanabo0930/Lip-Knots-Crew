# 読取同期とアプリ操作の競合防止

## 課題

スプシ書込をまだ有効にしていない状態で、管理画面からキャンセルや解除を行うと、次の読取同期で古いスプシ状態へ戻る危険があります。

## v0.9の対策

- アプリ操作時に `appOverride` を保存
- スプシがまだ追いついていない間は、アプリ側の状態を優先
- スプシ書込完了後、読取結果が一致したら `appOverride` を削除

## 対象

- キャンセル
- キャンセル解除

今後、スタッフ変更や募集停止などにも同じ仕組みを拡張できます。
