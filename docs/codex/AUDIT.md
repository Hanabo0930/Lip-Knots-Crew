# 全体監査と改善の順序

## 送信済み下書きの後片付け復旧（2026-09-05）

Storage転送がすべて完了した時点で送信済み印をメモリとlocalStorageへ記録し、選択ファイル・確認チェックを解除してDrive処理確認へ進む。IndexedDB削除だけの失敗をアップロード失敗にせず、独立した後片付け再試行を表示する。送信済み印がある間は旧下書きの復元・保存し直しを抑止し、実データの削除成功後だけ印を解除する。別会社・担当者の分離済みdraftKeyを使用。

実IndexedDBの削除abort後、新しいモジュール状態でもlocalStorageの印から復元抑止できること、古い保存の拒否、後片付け後の新規保存を検査。実アップロード後半ハンドラーを制御応答で実行し、端末失敗でもpolling継続・ファイル解除・認証変更後破棄を確認。Staffビルド・ブラウザ・UX回帰成功。

制限: IndexedDBとlocalStorageの両方が使えない場合は、同じ画面内のメモリで復元を抑止するが、ブラウザ再起動後まで保証できない。この場合は画面に提出履歴を先に確認するよう明示する。サーバー側の一意性保証・実回線転送の検証を追加したという意味ではない。後片付けを完了するまで、その提出先の新しい下書き保存も拒否し、選択中ファイルは画面に残す。


## 過去シフトの段階読込（2026-09-05）

過去履歴の初回取得を300件から51件（表示50＋続き判定1）へ減らし、snapshot cursorで50件ずつ追加する。既存の古い順取得を維持し、画面にも明記。未来の上限300件は独立のまま。1,000件が同じ日付でも抜け・重複なし、失敗時カーソル保持と再試行、連打、認証変更・一覧更新後の旧応答破棄を回帰検査。

未読込の過去タスクは案件1件を必要時取得し、会社・担当者・assigned状態・cancelledを確認してから一覧へ追加する。認証変更後の応答は破棄し、読込中の提出操作を抑止。データ書込・Rules・インデックス変更なし。

Staff型ビルド・UX127項目＋5シナリオ・提出復旧/ブラウザ検査成功。取得件数削減は設計上の上限比較であり、実回線の速度向上率は未計測。過去履歴は新しい順ではなく古い順に追加し、再読込では初回ページへ戻る。全履歴を端末へ永続キャッシュする機能は追加していない。


## 将来シフト欠落防止と全体画面一覧（2026-09-05）

fetchMyJobsを今日以降と過去の2クエリへ分離し、過去300件で将来シフトが押し出される問題を修正。日付は1回だけ取得し、両方に会社・担当者条件と上限300を維持。合成過去1000件＋今日・明日で取得、別会社/担当者除外、重複なし、通信失敗伝播を検査。既存dateKey昇順のインデックス定義を使用し、クラウド設定変更なし。

取得数は合計最大600件、クエリは1→2件。これは正確性の修正であり、読込速度の向上とは報告しない。過去履歴は従来の昇順300件を維持し、全履歴のページング・将来300件超の対応は未実装。

Staff/Adminビルド、Staff回帰・会社分離・ブラウザ、Admin全5画面の検査成功。Admin検査をWindows改行に対応。画面一覧はrelease-evidence/screens-20260905/index.htmlとoverview.png。現在の主要10画面と改善前後を保存し、画像拡大・Escape・390px表示を確認。初期会話の原画像は未特定。


## 下書き中断復旧と速度要件（2026-09-05）

IndexedDBのトランザクション中断を保存・解除・読込で処理し、待機が終わらない問題を修正。読込はリクエスト成功時ではなくトランザクション完了後に復元する。実DBの中断後データ保持・再試行と50MiB保存復元をブラウザ検査へ追加。型ビルド・ブラウザ・提出復旧・Staff UX回帰成功。端末保存失敗の案内から誤った通信状態の確認を除き、選択ファイルを画面から送信できることを明示。

[速度と業務効率の再点検](PERFORMANCE_REQUIREMENTS.md)で、将来シフト取得範囲と提出後の端末後片付け失敗を次の課題として特定。実回線の50MiB転送や実業務受入は未検証。


## Adminの日常業務改善と測定

- 「概要／案件／報告書・再提出／スタッフ／通知・運用」の5画面を追加。全20パネルを維持し、初回利用までマウントしない。利用後は非表示で保持し入力を消さない。
- 初期DOM: 2,127 → 154、初期JS取得: 10 → 7。いずれもChromium・1280px・新規ブラウザのデモ測定。実アカウント・実回線の応答時間を示す値ではない。
- 初期処理から運用データ11件を必要時読込へ移動。全体停止の取得・認証ガード・サーバー権限チェックを維持。部分失敗の案内と再試行、ユーザー変更時の取得データ初期化を追加。
- 390/1280pxで全5画面、検索入力の保持、案件→経費、キーボード切替を実ブラウザ確認。集計表の親グリッドが狭幅で押し広がる問題を修正。
- Admin entry: 341.9KB raw / 98.8KB gzip、Staff entry: 254.4KB raw / 78.7KB gzip。既定上限内。Admin初期JS全体のサイズそのものは少し増えている。
- ロジック検査は未表示・認証準備前・成功・通信失敗・破棄・アカウント変更を制御応答で確認。実ユーザーの権限別業務受入は未実施。

残る優先課題は、承認を伴う実スタッフ提出の一往復、大容量転送の実回線検証、運用受入。以下のAdmin整理が未着手という記述は過去の記録。


## 再開後の確認・改善

| 分野 | 確認と改善 | 検証・残る制限 |
| --- | --- | --- |
| 検証基盤 | fixtureの配信順序を修正し、通常ホームへの誤遷移を解消 | 未完了だったブラウザ検査が完走 |
| 操作・デザイン | 今日のタスクと次回シフトを通知設定より前へ。320pxでは提出種類と履歴を1列にし、復旧ボタンの高さ・折返し・文字色を調整 | ホーム/シフト/提出の320/390/1280px、ルート文字32pxとキーボード再取得成功。モーション軽減時はシマーも停止 |
| 転送・速度 | 失敗を検知したら新しい転送を開始せず、実行中の転送が終わるまで待つ | 6件/並列3の制御応答で後続3件が開始されないこと、成功時の並列上限と再実行を確認。実回線の50MB転送は未検証 |
| 下書き | 会社とログインUID、案件、種類、依頼を保存キーに含める | キー衝突・別会社・別ユーザー・種類・依頼を回帰検査。実IndexedDBでも他所有者への復元なし。旧キーは削除せず自動復元しない |
| CI | Staff UX、会社分離、提出回復、Chromium実ブラウザ検査をrelease-candidateへ追加 | 新HeadのGitHub CI結果はPR #95を確認 |

容量: Staff entry 254.4KB raw / 78.7KB gzip、Admin entry 337.5KB raw / 97.7KB gzip。指定上限内。実ユーザーの通信速度・業務完了は未実証。

次のまとまりはAdminの日常業務整理と、承認を伴う実スタッフ提出の一往復。今回はStaffの提出・復旧を中心に完結させ、Admin全体の構造変更や実送信を含めない。以下は再開前の履歴として残す。


基準: main `94e9fb9c73f8be47c30da2c0335f853cfd0bd2b4`。2026-09-05にソース・既存設計・CI構成を読み取り監査。全システムの動的監査や実ユーザーデータの監査を完了したという意味ではありません。

## Batch 48で修正

| 問題 | 根拠 | 変更と確認 |
| --- | --- | --- |
| 売場画像の再提出タスクから報告書を開く | Staff `openTask` は `kind === sales_floor` 以外をreportにしていた。サーバー `task-core.ts` は再提出の種類を `metadata.type` に格納 | 種類・依頼情報を検証して遷移。実ハンドラーを呼ぶ回帰ケース |
| URL未取得の画像を再試行できない | `SubmissionPreviewImage` の `!src` 分岐にボタンがない | 再読込・処理中表示を追加。HTML生成と非同期ハンドラー検査 |
| 連続再取得・ファイル変更後の応答が競合する | 再取得に同期ロックと応答世代の検査がない | 同期refロックと世代検査。成功・空・失敗・対象変更・破棄を検査 |
| 古い画像再取得が別の提出履歴を更新する | `refreshFilePreview` がawait後に認証・提出先を検査していない | アカウントと提出先を照合。古い応答を破棄するケースを検査 |
| デモの次回シフトが日付経過で消える | `demoJobs` が2026-07-20固定 | 起動時の翌日にし、日付と曜日を一致させる |
| 引継ぎの入口と実証状況が分散 | 既存READMEはv5.6機能一覧中心、会話の最新承認が入っていない | `CODEX_START_HERE.md` と本フォルダへ集約 |

## 次に進めるまとまり

| 優先 | 作業単位 | 具体的な調査・受入 | 現状 |
| --- | --- | --- | --- |
| P0 | 提出業務の一往復 | 実スタッフのコード到達、PWAログイン、売場画像/報告書、管理者確認、再提出完了を照合 | UI起動検査だけでは未達。実送信・データ操作の権限を確認して計画 |
| P1 | Staffの主要画面と復旧 | ブラウザでBatch 48、通信断、下書き復元、画像再取得、狭幅/文字拡大/キーボードを確認 | ローカルブラウザの取得タイムアウト。未完了検査を残す |
| P1 | 回帰検査をCIに組み込む | `test:staff-ux-performance`、company-scope、新提出検査をrelease gateに追加し実際に実行されることを確認 | 現行 `release-candidate.yml` はこれらを直接実行しない。このBatchではworkflow変更なし |
| P1 | Adminの日常業務を整理 | `apps/admin/src/App.tsx` の日常業務と公開/SLO/ROI/診断を分け、遅延読込と権限・空状態を維持 | 約4,000行に多数の業務・技術パネル。画面構成案と実測から着手 |
| P1 | 大きい提出・通信中断 | 50MB、複数ファイル、回線断、復帰、取消、二重確定を実測。poolの一件失敗後に不要な送信が続くか確認 | 現行は並列3。不要な転送と回復の設計を要検証 |
| P1 | 下書きの所有者分離 | IndexedDBキーが案件/種別/依頼を軸にしている。共有端末のアカウント変更で他人の下書きを復元しないか確認 | 脆弱性と断定せず、案件割当・ID一意性・破棄を含めて検証 |
| P2 | 冗長な資料と起動負荷 | 過去版ドキュメントの用途索引、未使用コードの参照、通信fan-out、bundleの読み込み時点を調査 | 過去版の一括削除や、Firebase chunk分割の再調整はしない |
| P2 | 必要機能の不足 | 既存機能を受入表へ照合し、実装がない/操作が届かない項目だけ追加 | 大量の新機能を仮定して増やさない |

## 今回の検証証跡

- Staff/Admin: TypeScript・Viteビルド成功。ローカル実行はNode 24.19.0であり、指定のNode 22との一致はPR CIで確認する。
- Staff UX: 127既存assertions + 5端末セッションシナリオ成功。
- 提出フロー: タスク種類、無効依頼、画像履歴のセッション分離、再試行の重複防止/失敗/旧応答、URLなしの再読込表示を検査。
- 容量検査: Staff entry 254.1KB raw / 78.6KB gzip、Admin entry 337.5KB raw / 97.7KB gzipで既定上限内。実通信速度の実測値ではない。
- boot循環なし、Admin startup 15項目、会社スコープ、demo 16ケース成功。
- 自動化安全検査: staging 26、source integrity 8、Functions automation、Gmail 23、bootstrap 18、Hosting 44ケース成功。すべてローカル検査でクラウド変更なし。
- Playwright: ブラウザ取得先の接続タイムアウトで実画面検査は未完了。`--browser` の追加検査もまだ完走を確認していない。操作成功・スクリーンショット検証済みとは報告しない。
- PR CIと以後のSHA検証はGitHub PRで追跡する。通常開発PRのマージは2026-09-05の包括承認に従い、現Headの必須CI成功確認後に自律実行する。

## 変更境界

今回の変更はStaffソース、ローカル検査、引継ぎ資料のみ。Adminソース、Functions、Firebase設定、依存バージョン、workflow、安全規則は変更していません。既存ドキュメントの出典と履歴を保ち、資料の古さを理由に機能や安全規則を削除しません。

## ファイル選択の復旧（2026-09-05）

再提出で不適切な形式・50MiB超・取消を選ぶと、既存の正常なファイルまで消える問題を修正。無変更の追加（重複・件数上限）でも既存ファイル・確認・転送表示を保持し、有効な変更時だけ確認を解除する。画像確認にはブラウザ標準のlazy読込とasyncデコードを指定し、原本・送信サイズは変更しない。実機での速度向上率は未計測。

実ハンドラーで取消、形式、50MiB境界、重複、20件上限、混在、差替、操作ロックを検査。実ブラウザで画像デコード・無効差替の保持・取消・PDFへの差替・確認解除・削除を確認。Staff型ビルド、UX127項目＋5シナリオ、320/390/1280px・文字拡大・既存提出復旧のブラウザ検査成功。検査セレクター誤記は修正して再実行成功。実送信・実データ操作なし。この記録時点ではPR/マージ/STAGING反映は未実行。

## 管理者の案件検索・段階表示（2026-09-05）

案件一覧へ事前連絡待ち／担当確定／キャンセルの絞込、NFKC・大小文字正規化・空白区切りAND検索、日付検索、条件クリアを追加。事前連絡待ちはassignedかつ取消でなくpreContactがない案件のみ。検索用文字列はjobs更新時にメモ化。一覧は50行ずつ表示し、条件変更で先頭へ戻し、件数減少時は有効ページへ補正。画面切替では条件を保持。取得済み件数を明示し、全サーバーデータの検索と誤認させない。

合成1万件・200ページで抜け/重複なし、状態・取消フラグ・全半角・複数語・空・最終ページ補正を検査。Node22の検索処理のみ20回、中央値0.34ms/p95 0.60ms。ブラウザ描画や実回線の値ではない。Admin型ビルド、390/1280pxの5画面・検索・条件保持・経費導線・キーボード検査成功。初期DOM154/JS7件を維持。既存の案件取得上限や他のセレクト選択肢は変更していない。

報告書の未提出一覧は未実装。現行のAdmin Job型にあるlateFirstSubmissionは完了状態と同義でないため、これだけで未提出扱いにしない。次回は取得APIの項目と状態の意味を確認してから判定を設計する。この記録時点でPR・マージ・STAGINGは未実行。

## 管理者の提出履歴・未確認表示（2026-09-05）

未提出判定の監査中に、履歴取得失敗を0件と表示し、案件・種類切替後の古い応答が残る問題を確認。履歴をidle/loading/ready/errorに分離し、現在の認証・案件・種類に対応した成功結果だけ表示する。取得開始時に元画像選択を解除し、同一対象の連打を抑止。応答順序・認証変更・effect cleanupの世代を確認し、古い成功/失敗/finallyを破棄。欠落したsubmissionsやfiles配列は成功0件ではなく取得失敗とする。

未確認・失敗・読込中には再提出依頼を無効化し、ハンドラー側にもガードを追加。成功してファイルが0件の場合だけ全体への依頼を許す。正常な0件でも業務上の未提出確定とは限らないため、今回の変更は全案件の未提出フィルターを追加したものではない。

実ハンドラーの成功/0件/失敗/異常応答/再試行/連打/案件変更/認証変更/応答逆転が成功。Admin型ビルドとブラウザ5画面・種類切替時の選択解除・再読込、既存1万件検索検査が成功。初期DOM154/JS7を維持。実通信による再提出依頼・データ書込なし。この記録時点でPR/マージ/STAGINGは未実行。

PR #104初回CIは旧認証ガード文の完全一致検査で停止。実装の認証ガードは維持されており、検査を現行条件へ更新し、未認証・対象未選択・Functions未準備ではAPIを呼ばない実ハンドラー検査を追加。ローカル起動安全検査と回帰成功。

## 報告書完了記録の絞込と履歴状態（2026-09-05）

auth.tsのfetchAdminDirectoryは会社条件付き・workDate昇順・100件で、serializeDocumentを通じsubmissionStatusも返す。submission-status.tsはreport.completed=trueを完了時に記録する。lateFirstSubmissionは遅延履歴であり完了判定には使わない。処理中はsubmissions/filesの別状態で、一覧APIに集約されていない。

assignedかつ取消でない案件を「報告書：完了記録あり」（completed===true）と「完了未確認」に分け、一覧列・絞込を追加。未確認には未提出・処理中・記録不足が含まれ、再提出依頼の完了は別であると明記。初回100件・読込済み範囲も明示。履歴には提出単位とファイル単位で処理状態を表示し、未知状態は状態未確認。0ファイルだけで未提出と断定しない案内へ変更。

完了true/false/欠落・遅延だけ・取消・未割当・未知処理状態の検査、実ブラウザの完了/未確認絞込と履歴表示、Admin型ビルド・起動安全15項目・既存5画面回帰成功。新規API呼出、サーバー/実データ変更なし。全サーバー案件を対象にした未提出と処理中の厳密な区分、100件超取得、再提出状態を統合した一覧は未実装。この記録時点でPR・マージ・STAGING未実行。

## 管理者の一覧取得・提出確認をまとめて改善（2026-09-05）

ユーザーは外出中の自律継続を明示。小さいPRごとの終了を避け、一覧100件上限・対象への導線・画像復旧・再提出送信を1つの変更へまとめた。Production/Functions/実送信/実データ変更の承認は拡大していない。

- STAGING現行Rulesがリポジトリと一致し、companyId + workDate ASCのjobsインデックスがREADYであることをメタデータだけで確認。証跡release-evidence/admin-paging-permissions.json。quota projectをリクエストヘッダーでSTAGINGへ明示。Rules/IAM/API設定変更なし。
- 初回bootstrapの100件を維持し、管理者の認証claimsのcompanyIdを確認して、既存Firestore読取権限で続き100件を必要時取得。初回の続き位置は最後の文書を再取得し、その後はsnapshot cursor。続き判定を含め最大101件。会社条件・追加の所属検査・IDでの重複統合。認証/一覧更新後の古い応答を破棄し、失敗ではカーソルを進めず再試行できる。
- 一覧から対象の報告書確認へ1操作で移動し、パネルへスクロールとフォーカス。入力した理由・補足・資料番号がある案件切替や、入力済み理由・補足がある種類切替は破棄確認。中止なら保持。同じ案件への移動で入力を消さない。再読込後に対象が初回100件から外れても確認中対象を明示する。
- 画像カードをプレビューと選択操作に分離。画像取得失敗・URLなしから履歴再取得へ進め、画像はlazy/async表示。処理完了ファイルのみ個別再送対象にでき、依頼ハンドラーでも現在の履歴との所属を確認。
- 再提出依頼は同期ロックで連打を抑止し、認証/対象切替後の旧結果を破棄。受付後の一覧更新失敗を送信失敗と混同しない。通信結果不明時は、再送前に依頼一覧を再読込して確認するよう案内。実依頼は送信していない。
- 検索・ページ操作・画像表示・追加取得コードを必要時読込に分割。Admin entry349.4KiB raw/101.6KiB gzip、最大chunk459.3KiB raw/135.2KiB gzipで既存予算内。初期DOM154/JS7を維持。SDKの読取機能追加によりFirestore chunkは増加しており、実回線速度が上がったとの主張ではない。

検査: 1,000件の同日合成案件を初回100件＋続きで漏れ/重複なく取得、会社/アンカー/失敗再試行/日時変換、認証と更新の競合、送信連打/結果不明/受付後更新失敗を実ハンドラーとSDK境界mockで検査。ブラウザでは390/1280pxの5画面、対象移動とフォーカス、画像失敗と復旧、破棄確認の中止/受入を検査。型ビルド・起動15項目・容量・循環依存検査成功。画面証跡はrelease-evidence/admin-review-20260905（デモのみ）。この記録時点でPR/マージ/STAGINGは未実行。

制限: 実管理者による追加Firestore読取は未実施。一覧再読込は先頭100件へ戻る。古い順を維持し、日付を変更・追加・削除する同時更新を固定したスナップショットとしては保証しない。workDateのない文書は既存orderByの対象外。全案件の処理中集約は追加していない。再提出のサーバー冪等性は追加せず、ネットワーク不明時の再送判断は一覧確認を要する。

## 公開設定ありの容量検査を追加（2026-09-05）

PR #106はmain 0f648223c0da4ff883f0270471b15b82c84657dbへマージし、PR/main CI成功。しかしPreview 33957551572で公開設定込みのAdmin entryが350.4KiBとなり350KiB上限を超過。Promoteはスキップされ、ライブはPR #105のまま。ローカルの未設定デモと公開設定によるサイズ差を見逃していた。

提出履歴表示を必要時読込へ追加分割し、合成の公開設定で--mode stagingのビルド容量を検査するcheck-admin-configured-budget.mjsを追加。既存test-admin-workspace --browserから呼ぶためPR/main CIでも同じ上限を検査する。合成設定はビルド専用出力release-evidence/configured-admin-budgetへ保存し、配信・認証・API呼出・デプロイをしない。実Hostingのdistを上書きしない。上限変更なし。

合成公開設定のAdmin entry348.8KiB raw/101.4KiB gzipで成功。通常型ビルド・ブラウザ・起動15項目も成功。依頼一覧再取得のエラー表示も認証ガード付きハンドラーへ接続。元の4機能改善は維持。この追記時点で修正PRと再公開は未実行。

## 将来シフトの続き取得と操作時間の基準値（2026-09-05）

将来シフトの取得が300件で打ち切られ、続きの存在も分からない問題を改善。初回は続き判定を含む301件を取得して300件保持し、シフト画面のボタンから50件単位で追加する。既存のcompanyId・assignedStaffId・dateKey条件と日付昇順を維持し、snapshot cursorを使う。追加は最大51件。失敗時はcursorを進めず、同期ロックで連打を抑止し、認証切替/一覧更新後の古い応答を破棄。追加読込はクラウド読取のみで、権限・インデックス・サーバー変更なし。

実ハンドラーをSDK境界mockで実行し、0/299/300/301件境界、同日を含む将来1,002件、既存の過去1,000件、再試行、認証/更新競合、連打を検査。実JSXのコントロールを使った320/390/1280px・キーボード・処理中無効化・失敗表示・再試行・最終ページでの消去をブラウザ検査。型ビルド・Staff UX127項目＋5シナリオ・会社分離・両アプリ容量検査成功。旧配列式への固定検査はslice(0,300)に更新。

新規scripts/test-staff-navigation-performance.mjsは、ビルド済みデモのホーム/シフト切替を390/1280px、各20回で測る。Chromium151.0.7922.34、CPU制限なし、localhost、外部通信禁止。DOM clickから対象表示＋2回のrequestAnimationFrameまで。中央値38.8〜41.3ms、p95 42.5〜43.2ms。ソースと配信JSのSHA256、全標本をrelease-evidence/staff-upcoming-20260905/staff-navigation-performance.jsonへ保存。小さい組込デモの読込済み切替の基準値であり、改善率・初回表示・実回線・大量案件描画の達成証明ではない。計測は任意実行で、環境依存の速度をCI必須閾値にはしていない。

制限: 実Staffログインによる追加Firestore読取は未実施。検索/件数は読込済み分。追加分は端末キャッシュに追記せず、一覧更新・再起動で初回範囲へ戻る。並行した日付変更・削除を固定スナップショットとして保証しない。全件カード描画と過去を新しい順に取得する改善は残る。この追記時点でPR/公開は未実行。
## 大量シフトの50件表示と原本要件の確認（2026-09-06）

100/300/1,000件の合成案件を実画面へローカルビルド限定で注入して計測。改善前1,000件はカード1,000枚・DOM6,072、390/1280pxのp95 75.7/80.7ms。50件単位のページ表示に変更後はカード50枚・DOM381、p95 42.1/43.0ms。各20回、同じChromium151、CPU制限なし、localhost、読込済み切替＋2フレーム。小標本であり初回表示・実回線の証明ではない。通信量は変更せず、取得と表示ページを分離する。

ShiftJobCardsにページ数補正とネイティブボタンによる選択を実装。ページを画面往復で保持し、別導線で対象が変われば対象ページを表示。選択後は詳細へスクロール/フォーカス。ページ移動で遅延フォーカスが次の操作を奪う競合は同期フォーカスへ修正。合成1,000件の全ページ重複/欠落なし、0/1/49/50/51/1,000境界、画面往復、キーボード、詳細一致を検査。既存提出復旧・狭幅/文字拡大・Staff UX127+5・取得競合回帰成功。新規検査をCIへ追加。

ユーザーから実業務スプレッドシートと連携仕様が共有された。原本は一切変更しない明示指示をAGENTS.mdへ記録。B列空欄で募集判定は確定。キャンセルのグレーアウト実例はなく専用色/範囲は未確定。背景色を読まない現行取込では未対応。列対応の不足を含めdocs/codex/LIVE_SHIFT_SHEET_REQUIREMENTS.mdへ記録。表はヘッダー/メタデータ/一部書式のみ読み、編集・取込・書戻しなし。この記録時点でPR/公開は未実行。
## 応募確定後の一覧更新失敗（2026-09-06）
応募API成功後のloadOpenJobs失敗が共通エラー表示へ流れ、応募に失敗したように見える問題を修正。成功直後に対象を募集一覧から除き、再取得失敗でも確定表示と「もう一度試す」の案内を維持する。認証切替後の応答は既存ガードで破棄。
実applyハンドラーの6ケース、Staff型ビルド、UX127項目+5、既存ブラウザの提出復旧・320/390/1280px・文字拡大・キーボード検査成功。新規test-staff-application-confirmation.mjsをCIへ追加。追加API呼出なし。Functions/GAS/原本変更なし。
API応答自体が失われた場合の再試行ID維持・結果照合は別課題。取消済みFirestore案件は応募transactionのstatus/cancelledチェックで拒否するコードを確認したが、スプシの未同期キャンセルを即時認識する証明ではない。実取込/実応募は実行していない。この追記時点でPR/公開は未実行。
## 応募応答喪失時の再試行（2026-09-07）
applyToJobはuid/requestIdで成功結果を保存しているが、画面が再試行ごとに新UUIDを作るため、成功済みの結果を照会できなかった。同じ認証世代・画面内の案件別MapでrequestIdと開始時刻を保持。API成功後は対象を削除、認証状態変更時は全削除。古い認証の応答は新しいMapを変更しない。サーバー記録のexpiresAt=24時間に対し23時間以上/時計逆行では再送せずシフト確認を案内。新しい番号を自動で振り直さない。
実applyハンドラーをSDK mockで実行し、最初だけ応答喪失/成功記録の再利用で確定書込1回を検査。別案件・認証切替・旧応答・23時間境界/時計逆行・前回の一覧失敗とデモを含む12ケース成功。Staff型ビルド、UX127+5、既存ブラウザ回帰（320/390/1280、文字拡大、キーボード、提出復旧）成功。
制限: メモリ内のみ。再読込/端末終了/認証状態変更をまたぐ結果回復は未実装。Firestoreの実TTL削除や本番同時応募の証明ではない。追加API/自動再送/Functions/GAS/原本変更なし。この追記時点でPR/公開は未実行。
## シフトの手動更新と応募結果確認（2026-09-07）
自分のシフト画面に「シフトを更新」と日付/店舗・追加ページの確認案内を追加。手動更新は30秒の自動更新間隔を迂回し、既存の将来301/過去51件クエリをgetDocsFromServerで取得する。会社/担当者条件は維持。成功時のみ更新表示、失敗時は以前の一覧を保持し応募結果は未確認と明記。未表示を応募失敗と断定しない。自動更新は従来getDocsを維持。
共有の同期refで手動/自動更新の多重実行を抑止、認証変更でリセット。古い応答で新認証のロック/表示を解除しない。手動更新は初期読込/応募/提出・ファイル処理中は抑止する。下書きが存在するだけでは停止しない。デモでは実応募結果でないと表示しAPIを呼ばない。
検証: 手動更新実ハンドラーのサーバー指定・自動間隔・連打・失敗/再試行・認証競合・操作中/未認証ガード・デモ。実fetchMyJobsのサーバー分岐2読取と会社/担当者条件・offline伝播。型ビルド、UX127+5、応募12ケース、取得競合回帰、実ブラウザ320/390/1280pxの手動更新/キーボードと既存提出復旧に成功。新規検査をCIへ追加。Firebase公式API仕様 https://firebase.google.com/docs/reference/js/firestore を照合。
制限: 結果不明だった応募を自動特定する機能やrequestId永続保存ではない。初回取得範囲外は追加読込が必要。更新で追加読込分は初回範囲へ戻る。手動操作により既存の2シフトクエリとタスク取得（募集一覧が起動済みならその更新）を実行する。実アカウントの読取/通信断は未受入。Functions/GAS/実原本/データ書込なし。この記録時点でPR/公開は未実行。
## 取消時の勤務枠所有者検証（2026-09-07）

再現: adminCancelJobと現行管理画面のadminSetJobCancellationは取消対象のassignedStaffId/dateKeyから勤務枠を組み立て、jobIdを読まずactive=falseを書いていた。旧案件を取消→同日別案件へ確定→旧案件を再取消すると新案件の枠を解除する。SDK模擬の実ハンドラー検査で修正前にanother job lockが失敗した。
修正: 全書込より前に同一transactionでstaffDayLocksを読み、active=trueかつjobId/companyId/staffId/dateKeyの一致時だけ解除。別案件・所属欠落/不一致・欠落/無効枠は変更しない。担当者なしは追加読取なし。取消本体と既存キューの意味は変更しない。取消時最大1読取増。整合しない旧枠は自動修復せず保持する。
検証: scripts/test-cancellation-lock-ownership.mjsは実jobs.ts/analytics.tsをTypeScript変換しSDK境界をmock化。所有枠/別案件/欠落/所有者欠落/会社・担当者・日付不一致/無効/再取消/会社・role拒否/未割当を両APIで計24ケース。読取前書込を拒否するmockでtransaction順序も確認。実Firestore競合・実応募・実送信を検証した証明ではない。functions CIへ追加。
範囲: Functionsソース・回帰・CI・本記録のみ。Functions反映、Rules/IAM/原本/GAS/実データ/実送信は実施しない。Hosting反映だけではこの修正は有効にならない。
残事項: shift-import.tsの勤務枠更新は別経路で、取消再取込時の所有者と日付変更の扱いを引き続き監査する。新規提出の取消後ポリシーは本人へ質問中。今回の修正を全取消経路の解決と扱わない。
現行AdminのApp.tsxはadminSetJobCancellationを呼ぶことを確認。analytics.tsでも同じ不具合を修正前の検査で再現してから修正。通知・取消理由/金銭区分・auditLogs・appOverrideの書込は保持。旧APIのみの修正ではなく両取消APIが対象。管理者担当者編集の旧枠解除は別経路として監査に残す。

## シフト取込の案件・勤務枠整合性（2026-09-07）

問題: 取込は案件を事前getAll後にBatchWriterで上書きし、旧勤務枠解除に新dateKeyを使い、枠所有者も未照合だった。取消再取込で同日別案件の枠を解除し、永続案件IDの日付変更で旧枠が残る。350書込境界で案件と枠が分断され、応募transactionとの競合で古い担当者状態へ戻り得た。
変更: 最大25案件単位のtransactionで最新案件を読取り、旧日付・新日付の必要枠を重複なく取得。全検査後に案件・旧枠解除・新枠確保を同transactionで書く。別案件の有効枠、所属/状態不明な新枠、同単位の二重手配はfailed-preconditionでその単位の全書込を止める。旧枠は会社/担当者/旧日付/案件の一致した有効枠だけ解除。欠落旧枠は作らない。既存他社案件は拒否。同一jobIdの重複入力は全書込前に拒否。
応募保護: applicationUnconfirmed=trueかつ非取消assignedのアプリ応募は、原本の担当者/日付/assigned状態が一致するまで古い空欄・別担当者・日付で消さず取込を止める。原本一致時に同フラグをfalseにする（applicationAdminConfirmedとは別。管理者確認を書き換えない）。明示の取消と既存appOverrideのcancel/restore状態優先は維持。担当者データ全体や他の業務項目の同期競合解決ではない。
検証: scripts/test-shift-import-transactions.mjsで実writeJobsAndLocksを境界抽出しSDKの読取/書込/再試行を模擬。修正前は他案件枠保持のケースが失敗、修正後43ケース成功。日付/担当者変更、取消、他社/曖昧枠、appOverride、未反映応募、競合再試行で応募/枠を読み直し、commit失敗、重複ID/手配、0/1/25/26/351件、後続単位失敗、読取結果不足を含む。Functions型ビルド成功（初回のnoUncheckedIndexedAccess検出は不足時明示拒否を追加して解消）。既存取消24ケース成功。新テストをFunctions CIへ追加。
制限: 取込全体のall-or-nothingではない。後続単位失敗時、先行の正常完了単位は残る。最大25案件/75書込、読取は案件25+旧/新枠最大50（重複除去）で、再試行時は増える。合成351件は15transactionに分割。旧batchより実速度が速いとは主張しない。実Firestore競合/負荷/原本取込は未受入。取込leaseと最大所要時間、管理者担当者変更の旧枠解除、既存破損枠の修復、cancel/restore時の担当者優先順位は別途監査。原本未確定・取消列未確定のまま同期を有効化しない。
反映: Functionsソースの準備のみ。原本/GAS/Functions/Rules/IAM/実データ/実送信は変更しない。Hosting反映はFunctionsの有効化ではない。
公式仕様: https://firebase.google.com/docs/firestore/manage-data/transactions （全読取を全書込より先に行い、競合時はcallbackが再実行される）。
設定会社の固定: loadConfigのcompanyIdの後に保存データをspreadしていたため保存された他会社IDが優先される問題も、実ConfigSchema/loadConfigの合成検査で再現して修正。保存companyIdが存在して不一致ならfailed-precondition、欠落は呼出元会社に固定、既存一致/欠落設定/不正設定/旧設定を5ケースで検査。実設定・権限は変更しない。

## 管理者担当者変更の旧勤務枠保護（2026-09-07）
adminEditJobInputsは担当者解除/変更で旧枠のjobIdを検査せずactive=falseを書いていた。別案件で使われている旧担当者の同日枠を解除するケースを、実EditSchemaとハンドラーの境界抽出・SDK模擬で修正前に再現した。
修正: 担当者を実際に解除/変更する時だけ同transaction内で旧枠を読取り、有効状態・会社・担当者・日付・案件が一致する場合だけ解除。欠落/不整合/別案件の旧枠は保持。全読取は書込より先。新担当者側の既存チェック、revision、編集内容、書戻し保留/キュー、監査の意味は変更しない。
検証: scripts/test-admin-reassignment-locks.mjsの23ケース成功（解除/変更それぞれ8条件、同担当者・非担当者項目、revision/role拒否、変更先競合、旧枠変更後の再試行、workDate fallback）。Functions型ビルド、既存取込43・取消24ケース成功。Functions CIへ追加。実DB/シート/送信は使っていない。
担当者変更時最大1読取増、通常項目編集/同担当者なら追加なし。既存破損枠の修復・取消済み案件の再手配方針・新枠の不正メタデータ・監査書込失敗時の再試行は今回の対象外。Functions未反映。Hosting反映だけでは本修正は有効にならない。
## 勤務枠診断・取込リース・対応手順の一括改善（2026-09-07）
ユーザーは小さな修正単位で止めず関連作業をまとめることを希望。ローカル読取専用診断、取込重複保護、合成例、対応手順、再実行可能な性能計測を1PRに統合。
診断: scripts/diagnose-shift-integrity.mjsはJSONのみ読取り、会社混在/重複文書IDを拒否。欠落/無効/別案件枠、二重手配、孤立枠、取消/未手配の有効枠、担当者/日付不一致、曖昧状態を検出。complete.jobs/locksが不明なら不在を異常と断定しない。CLIは既存出力を上書きせず、標準出力は件数のみ。レポートはID/コード/重要度のみ（余分な氏名・メール・金額を転記しない）。データ取得/修復/API/自動送信なし。
取込: 最大540秒に対しリース480秒だった不一致を600秒へ修正。取得transactionの再試行時点で期限を再計算。各25案件transactionでリースを読取り、会社/token/Timestamp期限を照合し、期限切れ/交代/欠落時はabortedでその単位を停止。リース文書もtransactionで読むので交代と競合時に再検証される。自分のtoken以外の解放は従来どおり禁止。更新単位最大読取75→76、失敗時の先行正常単位保持は継続。リース判定は各単位の開始時点。実行上限変更/新規トリガー/スケジュール有効化はしない。
検証: 型ビルド、取込48・リース6・診断24・担当者変更23・取消24の計125ケース成功。合成1万件ずつの診断を含む。実SDKの負荷/競合/実原本取込の受入ではない。benchmark-shift-integrity.mjsで1000/10000件を各20回測定、正本release-evidence/shift-integrity-benchmark.jsonに全標本・ソースSHA256を保存。数値はメモリ上照合のみでI/O/クラウドを含まない。
操作資料docs/codex/SHIFT_INTEGRITY_GUIDE.md、合成入力config-samples/shift-integrity.synthetic.json、診断例release-evidence/shift-integrity-example.json（3指摘）を保存。Functions未反映。原本/GAS/Rules/IAM/Production/実データ/実送信変更なし。手動/定期取込両方のFunctionsを同版に揃える反映は現在の範囲外で未実施。
## STAGING読取診断と反映準備（2026-09-07）
read-staging-shift-integrity.mjsを追加。固定STAGING/(default)、明示companyId、jobs/staffDayLocksの必要項目のみを同一readOnly transactionで照合。上限10000/種、超過時は失敗。会社/文書範囲/重複/応答不足/型/取得失敗を拒否しrollbackで終了。標準入力の短期トークンは保存/出力しない。レポートのみ保存し原データを残さない。両方0件はunverified。会社条件に一致しない文書は範囲外であり全DBの整合を保証しない。
実読取: 既存gcloud認証を使いlipknots会社のjobs=0/locks=0、unverifiedを確認。実原本/GAS/実同期/データ書込/送信/権限変更なし。証跡release-evidence/staging-integrity-read-20260907.json。空の範囲なので実業務データの診断受入は未完了。認証操作は既存短期トークン取得のみ、新規ログイン/鍵/権限なし。
検証: 取得側30ケース（API形状・固定接続先・同transaction・projection・欠落枠・0件・会社混在・上限・異常応答・途中失敗・rollback失敗・個人項目除外・トークン秘匿・CLI上書き拒否/失敗出力/接続先追加拒否）、既存診断24ケース成功。取得検査をCI追加。Firestore実接続は空範囲の受入のみ、非空の競合/大量データは合成検査。
SHIFT_INTEGRITY_GUIDEを更新、SERVER_ROLLOUT_READINESSへPR #113〜#116の未反映関数・同版/定期実行/限定合成データ・復旧条件を整理。Functions許可リストやデプロイワークフローの権限範囲は変更しない。Hostingの反映はFunctions稼働証跡にしない。

## 管理者変更先勤務枠の整合検査（2026-09-07）
adminEditJobInputsの新勤務枠は従来active=trueかつjobIdが違う場合だけ拒否しており、同案件を名乗る別会社/担当者/日付の枠や、active不明・不正な無効枠を上書きできた。回帰ケース追加で修正前の拒否不足を再現。
変更先の既存枠はcompanyId/staffId/dateKey/activeの型を照合し、不一致はfailed-precondition。有効別案件の既存拒否は維持。有効な自案件枠、所属情報が一致する無効枠、欠落枠は利用可能。手配日付は文字列YYYY-MM-DDと実日付の往復検証で確認し、欠落/不正は拒否。旧workDateのfallbackは維持。同transaction内の検査で書込前に止め、再試行時の新枠所有者交代も再照合する。読取回数の追加なし。日付のない案件の通常項目編集や担当者解除の意味は変更しない。
検証: 実EditSchema/handlerを使うSDK境界模擬43ケース（従来23+新20）、取消24、取込48の計115ケース成功。新規ケースは有効/無効の会社・担当者・日付不一致、状態不明、正常再利用、日付不正/旧形式fallback、競合再試行。旧枠・案件・変更先の保持と監査未書込も確認。実DB競合/本番の受入ではない。Functions未反映。原本・実データ・GAS・送信・Rules・IAM変更なし。

## Hosting準備処理の効率化（2026-09-08）
差分だけで再配信を省略する案は、公開設定の変更・前回反映版・稼働サイトの差分を照合する根拠が不足するため今回は導入しない。最新main/同SHA CI/Preview/guard/保護環境/バックアップ/同版promote/公開後検査を維持する。
PreviewのStaff/Admin stagingビルドをbuild-staging-hosting.shで並行実行。別dist・別tsconfig（referencesなし/noEmit）を確認。両方のPIDをwaitし、片側または両側の失敗を非0に集約してサイズ検査・配信へ進ませない。npmコマンドは2対象だけに固定。Promoteでは引き続き再ビルドしない。新シェルスクリプトのLFを.gitattributesで固定。
Preview/PromoteのPlaywright取得を--only-shellへ。現行検査はchromium.launch({headless:true})でchannel指定なしのため、未使用のフルChromium取得を省く。根拠: https://playwright.dev/docs/browsers 。固定バージョンのChromium Headless Shell・OS依存・画面検査自体は維持。
ローカル検証: 実Bash+模擬npmで双方同時起動、成功、Staff失敗、Admin失敗、両失敗、後続停止を5ケース確認。既存Hosting44ケース成功。H直下の初回既存テストはplaywright依存なしで起動不可、H正本から固定依存付きCキャッシュへ準備して再実行成功。両アプリの実stagingモードビルドとバンドルサイズ検査成功。ローカルは合成/デモ設定であり公開設定の受入はGitHub Previewで別に実施する。
ローカル成果物比較: 同設定/同ソースの並行/逐次で34ファイルすべてSHA256一致。単回・暖機済み環境で逐次10.153秒、並行7秒（Bash秒単位）。有意な速度保証/本番性能値ではない。release-evidence/hosting-build-efficiency-local.jsonに結果保存。

## 管理者操作結果と一覧更新エラーの分離（2026-09-08）
createJobGroup/duplicateJob/changePublication/saveJobEditのAPI成功後にloadJobsが失敗すると、成功・警告表示が読取例外に置き換わる問題を再現。refreshJobsAfterActionへ結果表示と一覧読取を集約し、一覧失敗だけを捕捉して元結果と「一覧を再読込」への案内を表示する。同操作の繰返しを促さない。既存の一覧再読込は読取だけで操作APIを再送しない。
API失敗は従来のcatchを維持して成功扱いにしない。作成後フォーム初期化、保存後revision更新、公開制限の警告、スプシキュー/保留の意味を維持。デモ経路はAPI/一覧取得を呼ばない。追加API呼出なし。保存自体の応答喪失やサーバー側の監査失敗、他の操作経路は本修正の対象外。
実ハンドラーをTypeScript変換して20ケースのSDK境界模擬検査。4操作それぞれ一覧成功/一覧失敗/API拒否/デモ、スプシキュー/保留、作成警告/公開制限を確認。修正前はREAD_FAILEDが作成完了表示を消す検査失敗、修正後成功。CIへ追加。原本/実データ/GAS/実送信/Functions/Rules/IAM変更なし。

## 管理者4操作の連打・認証切替保護（2026-09-08）
案件作成/複製/公開変更/入力保存を共通runAdminJobActionで包み、useRefのtokenをawait前に確保。同一画面では4操作全体を1件ずつ処理し、処理中の追加呼出を抑制。失敗/確認キャンセルでも自分のtokenを解放する。認証通知時にtokenとbusyをリセットし、旧利用者の結果・エラー・フォーム初期化・revision更新を適用しない。同一Userオブジェクトで認証通知が来た場合も失効する。
一覧取得にも同じ有効性判定を渡し、一覧更新途中の認証切替で古い結果を適用しない。旧処理のfinallyは自分のtoken以外を解除せず、新利用者のbusyも変更しない。既存の成功結果保護・キュー/警告表示・デモのAPI未呼出は維持。
合成40ケース（既存20+連打/認証切替/遅い一覧失敗/旧新token競合/失敗後解放/未ログイン/確認キャンセル20）成功。実ハンドラーと認証リセット部分を抽出して検証。実装途中のテンプレート文字列エスケープ誤りはテストで検出し修正後再実行成功。通信応答喪失後の手動再送、別タブ/別端末の二重実行、サーバーの冪等性は未解決。原本/実データ/GAS/実送信/Functions/Rules/IAM変更なし。

## 保存中の編集案件切替保護（2026-09-08）
saveJobEditActionの遅い成功応答が別案件のjobEditRevisionを上書きする問題を再現（新画面revision9→旧保存response3）。loadJobEditごとにjobEditContextRefを増やし、保存開始時の世代と既存認証/操作tokenの両方が一致する場合だけrevision・成功/失敗表示・一覧結果を反映する。別案件だけでなく同じ案件を開き直した場合も旧応答を除外。編集画面を開く際は前のメッセージを消す。
busy解除は既存の操作token判定を維持し、同一利用者が編集先を切り替えても旧保存終了後はbusyが解除される。既送信のサーバー保存は取り消さない。原案件の保存結果確認が必要な場合は一覧を読取り再確認する。単なる同一画面内のフィールド追加入力は世代を変えず、今回の対象は編集画面を開き直す操作。
実ハンドラーを使う合成45ケース（従来40+別案件/同案件の遅い成功/遅い失敗と一覧取得中切替5）成功。旧応答が新画面の入力/ID/revision/メッセージを変更せず、busyと操作tokenが解放されることを確認。原本/実データ/GAS/実送信/Functions/Rules/IAM変更なし。

## 2026-09-08 募集→応募→取消→再同期の結合検証
実モジュール全体を同じ合成DBへ接続して3失敗を再現。adminCancelJobの取消override不足による確定/募集への巻戻りと、取込元読取失敗の誤成功を修正。8結合シナリオ、seed接続先ガード23ケース、取消24/取込48/リース6回帰、Functions型ビルド成功。詳細・保証範囲は[SHIFT_LIFECYCLE_LOCAL.md](SHIFT_LIFECYCLE_LOCAL.md)。Java/Emulatorキャッシュ未確認のため今回はインメモリ境界であり、実SDKの並行transaction・Auth署名検証・実通知は保証しない。Functionsは未反映。Hostingと業務受入を区別する。

## 2026-09-08 提出・再提出の結合検証
提出→管理者確認→再提出→新旧比較→管理者完了を実モジュール全体で接続。再実行による二重加算・Drive再コピー、後片付け失敗による完了状態消失を再現し、転送/加算/案件反映のチェックポイントで修正。7合成シナリオ成功。詳細は[SUBMISSION_LIFECYCLE_LOCAL.md](SUBMISSION_LIFECYCLE_LOCAL.md)。Drive成功直後のチェックポイント未保存・同時イベントの重複コピーは未保証。旧版転送済みデータは自動再加算せず停止。Functionsは今回未配備、実ログイン・実送信・原本変更なし。
