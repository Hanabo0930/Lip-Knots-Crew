import assert from 'node:assert/strict';import fs from 'node:fs';import {runInNewContext} from 'node:vm';
const read=p=>fs.readFileSync(new URL('../../'+p,import.meta.url),'utf8').replace(/\r\n/g,'\n');
const guard=read('scripts/automation/check-function-auth-guards.mjs').replace(/^import .*;\n/gm,'');
const modules={getPilotReadiness:'sheet-row-creation',getPilotExpansionReview:'pilot-expansion',getProductionControlStatus:'production-control',getProductionSloDashboard:'production-slo'};
const sources=Object.fromEntries(Object.values(modules).map(m=>['functions/src/'+m+'.ts',read('functions/src/'+m+'.ts')]));let cases=0;
function run(name,change){const files={...sources};change?.(files);const lines=[],process={argv:['node','guard','--functions',name,'--require-pass'],exitCode:0,exit:c=>{throw Error('unexpected exit '+c);}};runInNewContext(guard,{process,console:{log:x=>lines.push(x),error:x=>lines.push(x)},execFileSync:(cmd,args)=>{assert.equal(cmd,'git');assert.equal(args[0],'show');const file=args[1].slice(args[1].indexOf(':')+1);assert.ok(Object.hasOwn(files,file));return files[file];}},{timeout:3000});return{passed:lines.includes('SOURCE_GUARD_STATUS=PASS'),exitCode:process.exitCode};}
function reject(name,before,after,{whole=false}={}){const file='functions/src/'+modules[name]+'.ts',source=sources[file],start=whole?0:source.indexOf('export const '+name),end=whole?source.length:source.indexOf('\n});',start)+4,block=source.slice(start,end);assert.ok(block.includes(before),name+': target absent '+before);assert.deepEqual(run(name,files=>{files[file]=source.slice(0,start)+block.replace(before,after)+source.slice(end);}),{passed:false,exitCode:1},name+': '+before);cases++;}
for(const name of Object.keys(modules)){assert.deepEqual(run(name),{passed:true,exitCode:0},name);cases++;reject(name,'requireAdmin(request)','unverified(request)');reject(name,'requireAdmin(request)','(await db.collection("bad").add({}), requireAdmin(request))');reject(name,'companyFromClaims(session.token)','request.data.companyId');}
for(const before of ['typeof mapping?.spreadsheetId === "string"','mapping.spreadsheetId.length > 0','mapping.spreadsheetId.trim() === mapping.spreadsheetId','mapping.monthCreation?.verifiedSpreadsheetId === mapping.spreadsheetId','blockedRows.empty && deadRows.empty','monthInterventions.empty'])reject('getPilotReadiness',before,'true');
reject('getPilotExpansionReview','reviewSnap.data()?.companyId === companyId','true');
for(const collection of ['pilotHealthRuns','pilotAlerts'])reject('getPilotExpansionReview',`db.collection("${collection}").where("companyId", "==", companyId)`,`db.collection("${collection}")`,{whole:true});
reject('getPilotExpansionReview','snap.data()?.companyId === companyId','true',{whole:true});
for(const before of ['review.data()?.companyId === companyId','rehearsalFingerprint: certification?.exists && certification.data()?.companyId === companyId','pendingApprovalSnap.data()?.companyId === companyId'])reject('getProductionControlStatus',before,before.startsWith('rehearsalFingerprint')?'rehearsalFingerprint: certification?.exists':'true');
reject('getProductionSloDashboard','openIncident.data()?.companyId===companyId','true');
reject('getProductionSloDashboard','.where("companyId","==",companyId)','.where("companyId","==",request.data.companyId)');
const config=JSON.parse(read('config/automation/staging-safety.json'));assert.ok(Object.keys(modules).every(n=>config.allowedFunctions.includes(n)&&!config.invokerMutableServices.includes(n.toLowerCase())));cases++;
console.log(JSON.stringify({readinessGuardTests:cases,functions:Object.keys(modules),cloudOperations:false}));
